# Teleconsulta Emilio — Frontend
Desplegar en Vercel como `teleconsulta-emilio.vercel.app`.

**Variable necesaria** en Vercel:
- `NEXT_PUBLIC_BACKEND_URL` = URL pública del backend en Railway

Scripts:
- `npm install`
- `npm run dev` (local)
- `npm run build` y `npm start` (producción)
